# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Environment variables (Just in case you add a backend later)
.env
.env.local
.env.development
.env.test
.env.production

# Node modules (If you ever add package.json)
node_modules/
npm-debug.log
yarn-error.log

# 🚨 CRITICAL SECURITY EXCLUSIONS 🚨
# Do not upload the Admin portal to public version control. 
# Keep this file local to your machine to prevent unauthorized database writes.
Admin.html
admin.html